<?php use Phalcon\Tag; ?>
<?php $this->flashSession->output() ?>

<div class="container">
  <div class="row">
    <div class="col-md-11 contentstd" style="margin-top:40px">
      <div class="page-header">
        <form class="form-inline">
          <label class="pull-left mb-5 mr-sm-5 mb-sm-0"><h1>รายชื่อนักศึกษา</h1></label>
          <input class="form-control mb-2 mr-sm-2 mb-sm-0" type="text" placeholder="Search">
          <button class="btn btn-outline-primary mb-5 mr-sm-5 mb-sm-0" type="submit">Search</button>
        </form>
      </div>
      <table class="table table-striped" style="margin-top:10px">
        <thead>
          <tr>
            <th>Id</th>
            <th>ชื่อ</th>
            <th>นามสกุล</th>
            <th>ชั้นปี</th>
            <th >แก้ไข</th>
            <th>ลบ</th>
          </tr>
        </thead>
        <tbody>

          <?php foreach ($page->items as $act): ?>
            <tr>
              <th scope="row"><?=$act->id; ?></th>
              <td><?=$act->Firstname?></td>
              <td><?=$act->Lastname?></td>
              <td><?=$act->Years?></td>
              <td><button type="button" class="btn btn-info">แก้ไข</button></td>
              <td><button type="button" class="btn btn-danger">ลบ</button></td>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
