<?php use Phalcon\Tag; ?>
<?php $this->flashSession->output() ?>

<div class="container">
  <div class="row">
    <div class="col-md-11 contentstd" style="margin-top:20px">
      <div class="page-header">
      <br>

      <label class="pull-left"><h1>รายชื่อนักศึกษา</h1></label>
      <input class="form-control pull-right" type="text" placeholder="Search">
      <button class="btn btn-outline-success  pull-right" type="submit">Search</button>
      <?= $this->tag->linkTo(["manage/studentadd","class"=>"btn btn-success pull-right","Add+"]); ?>
      </div>

      <table class="table table-striped" style="margin-top:10px">
        <thead>
          <tr>
            <th>Id</th>
            <th>ชื่อ</th>
            <th>นามสกุล</th>
            <th>ชั้นปี</th>
            <th >แก้ไข</th>
            <th>ลบ</th>
          </tr>
        </thead>
        <tbody>
          
          <?php foreach ($page->items as $act): ?>
            <tr>
              <th scope="row"><?=$act->id; ?></th>
              <td><?=$act->Firstname?></td>
              <td><?=$act->Lastname?></td>
              <td><?=$act->Years?></td>
              <td><button type="button" class="btn btn-info">แก้ไข</button></td>
              <td><button type="button" class="btn btn-danger">ลบ</button></td>
            </tr>
          <?php endforeach; ?>
        </tbody>
      </table>
    </div>
  </div>
</div>
